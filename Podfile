# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RecombineExample (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Sourcery'
  # Pods for RecombineExample (iOS)

end

target 'RecombineExample (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Sourcery'
  # Pods for RecombineExample (macOS)

end
